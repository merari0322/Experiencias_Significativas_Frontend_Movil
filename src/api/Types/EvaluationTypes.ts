export interface EvaluationDTO {
  // Define aquí los campos de la evaluación
  id?: number;
  name: string;
  // ...otros campos
}

export interface EvaluationRegisterDTO {
  // Define aquí los campos para crear una evaluación
  // ...campos
}
